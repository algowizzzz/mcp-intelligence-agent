{
  "name": "get_var_contribution",
  "implementation": "tools_pack_impl.var_contribution.VarContributionTool",
  "description": "Retrieve Value-at-Risk (VaR) contribution data including 95%, 99% and stress VaR per counterparty",
  "version": "1.0.0",
  "enabled": true,
  "inputSchema": {
    "type": "object",
    "properties": {
      "counterparty": {
        "type": "string",
        "description": "Filter by counterparty name (optional). Returns all VaR records if omitted."
      },
      "_worker_context": {
        "type": "object",
        "description": "Injected by agent — worker scope dict"
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "var_data": {"type": "array"},
      "_source": {"type": "string"}
    }
  },
  "metadata": {
    "author": "B-Pulse",
    "category": "Risk Data",
    "tags": ["VaR", "stress", "risk"]
  }
}
